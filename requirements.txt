# Core Libraries
pandas
numpy
scikit-learn
matplotlib
tqdm
joblib
optuna

# PyTorch (Do not install here; installed via `setup.sh`)
# PyTorch Geometric (Do not install here; installed via `setup.sh`)

# Visualization
streamlit
seaborn

# Additional
umap-learn
